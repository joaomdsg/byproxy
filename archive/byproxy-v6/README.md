# byproxy v6 — the archived ceremony

This is the project's measured-and-refuted ancestor, preserved runnable:
the contract-driven guard layer (orchestrator + explorer fleet + tool-less
critic + builder + cold auditor) in its final form, v6. Benchmark 7
(`benchmarks/2026-07-13-sonnet-refuted-fable-lean.md`) refuted its bet
twice and its successor — **nullius**, in `.claude/skills/nullius/` —
replaced it.

It stays in the tree because the graveyard is the argument: the harness's
`byproxy`, `byproxy-noaudit`, and `byproxy-nobuilder` arms wire these
files into their worktrees so every historical benchmark remains
reproducible. Do not install these agents/skills into a live `~/.claude`;
they are exhibits.
