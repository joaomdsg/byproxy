# agri-alert-via-port task metadata.
# A large SPA→Via v0.7 port: the arm reads a big mounted source frontend for
# depth (the scale lever) and conforms to the generic CONTRACT.md surface.
TASK_NAME=agri-alert-via-port
TASK_KIND=port
TASK_LANG=go
TASK_WORKDIR=skeleton
HIDDEN_DIR=hidden
# SCORE_MAX is advisory; score.sh derives the true total from the TestT* count.
SCORE_MAX=24
# Compaction window: tight relative to the mounted source so a solo arm cannot
# hold the whole frontend in context — forcing delegation (the discriminator).
CAP=64000
